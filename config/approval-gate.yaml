id: approval-gate
name: Approval Gate
description: 'Policy and decision surface settings: the deployment approval defaults (permission mode for new sessions and the auto-mode trust seed) and the agent permission rules.'
value:
  default_mode: manual
  grant_reask_limit: 3
  rules:
  - '!approval::*'
