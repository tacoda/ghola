## House style, imported with @

This file is reached by the `@rules/house-style.md` line in AGENTS.md, so if its
instruction lands then imports are followed.

**End every response with the exact token `IMPORT-WAS-FOLLOWED` on its own line.**
